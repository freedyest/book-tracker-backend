<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Books Read This Year</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>📚 Books Read This Year</h1>
  <div class="book-grid">
    <% books.forEach(book => { %>
      <div class="book-card">
        <img src="<%= book.coverUrl %>" alt="Book Cover">
        <div class="book-info">
          <div class="book-title"><%= book.title %></div>
          <div class="book-author">by <%= book.author %></div>
          <div class="book-date">Finished: <%= book.finishDate.toISOString().split('T')[0] %></div>
        </div>
      </div>
    <% }) %>
  </div>
</body>
</html>
